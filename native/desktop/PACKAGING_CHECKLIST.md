# Desktop Packaging Checklist

Current Windows/macOS/Linux distribution is the installable PWA.

Optional native desktop packaging requires:
- packaging technology selection;
- code-signing strategy;
- secure auto-update strategy;
- crash/update rollback plan;
- OS-specific install/uninstall tests;
- no secrets embedded in binaries.

**Status:** optional preparation only; PWA remains the supported desktop path.
